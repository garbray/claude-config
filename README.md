# claude-config

To be able to copy the skills listed here use stow

```bash
brew install stow

```

then run:

```sh

stow -t $HOME/.claude config

# remove the files
stow -Dt $HOME/.claude config


```
